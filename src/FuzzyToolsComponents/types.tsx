export type FuzzyTokensData = {
  tok_name: string
  tok_symbol: string
  init_supply: string | number
  tok_addr: string
  dev_addr: string
  dep_time: string
  eip_score: number
}

export interface Toasty {
  description: string
}
